# Movies
A simple full-stack Movie Browser application built using React and Node.js. The application loads movie data from a JSON file and provides APIs to list movies and retrieve movie details by ID.
